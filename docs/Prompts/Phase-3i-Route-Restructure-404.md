> Generated by Wolfgang — Clockwork Otter Foundry website, Phase 3i of the V1 roadmap (3h–3m). Route/URL mechanics and the 404 page, per the authoritative V1 content brief's Final V1 Route Map. Wolfgang read the live repository directly before writing this prompt (not from memory of a prior phase's report) after a tracking error on the previous phase — see `methodology/LESSONS_LEARNED.md`, 2026-08-10 entry — so the file states and hrefs described below should match what's actually in the repo.
> Do not edit this header.

# Phase 3i — V1 Route Restructure + 404 Page

**Project:** Clockwork Otter Foundry Website
**Phase:** 3i (interstitial, continues 3b–3h numbering; third phase of the V1 roadmap)
**Date:** 2026-08-10

---

## How to Execute This Prompt

Open a **new Claude Code session launched from this repository's root.**

Read this repository's `CLAUDE.md` first.

This phase is purely route/URL mechanics and the 404 page — **no content changes**. The Privacy Policy and Terms of Use pages keep their exact current placeholder text; only their file location/URL changes. Real legal copy is Phase 3j, not this one.

---

## Goal

Bring the site's URL structure in line with the brief's Final V1 Route Map:

```text
/
├── antiphon/
├── about/
├── contact/
├── privacy/
├── terms/
└── 404
```

Two routes need renaming (`/privacy-policy` → `/privacy`, `/terms-of-service` → `/terms`), every internal link needs to consistently use the trailing-slash form the map shows, and a real 404 page needs to exist — Phase 3h deleted `/products/antiphon` and `/documentation` without any redirect (correct — no server-side redirect capability exists on GitHub Pages without extra tooling, and this site isn't live/indexed yet), so those now need a real 404 to land on instead of a host-default error page.

---

## Project Context

Wolfgang verified these directly against the live repository before writing this prompt:

**`astro.config.mjs`** — currently:
```js
export default defineConfig({
  site: 'https://clockworkotterfoundry.com',
  vite: { plugins: [tailwindcss()] },
});
```
No `trailingSlash` or `build.format` option is set — both are on Astro's defaults (`trailingSlash: 'ignore'`, `build.format: 'directory'`). Under `build.format: 'directory'`, a page at `src/pages/about.astro` builds to `dist/about/index.html`, which is already the exact directory shape the brief's route map shows. **This means the map's structure may already be satisfied by Astro's default output with zero config change** — verify this directly by running a build and inspecting `dist/`, don't assume either way.

**Current pages needing a route rename:**
- `src/pages/privacy-policy.astro` (currently `<BaseLayout title="Privacy Policy">`, placeholder body `<h1>Privacy Policy — placeholder</h1>`) — needs to become `/privacy`.
- `src/pages/terms-of-service.astro` (currently `<BaseLayout title="Terms of Service">`, placeholder body `<h1>Terms of Service — placeholder</h1>`) — needs to become `/terms`. Its `title` prop should also change to `"Terms of Use"` — Phase 3h already renamed this page's *link label* everywhere it's referenced (footer says "Terms of Use"), but the page's own `<title>` tag still says "Terms of Service"; fix the inconsistency now since you're already renaming the file.

**Current internal links that need updating to trailing-slash form**, found by Wolfgang via direct grep — verify this list is complete before you finish, don't trust it as exhaustive:
- `src/components/Navigation.astro`: `links` array — `/antiphon`, `/about`, `/contact` (not `/`, which stays `/`)
- `src/components/Footer.astro`: `links` array — same three, plus `/privacy-policy` → `/privacy/` and `/terms-of-service` → `/terms/`
- `src/pages/index.astro`: the hero CTA (`href="/antiphon"`) and the Antiphon-introduction section's CTA (also `/antiphon`)

`src/pages/antiphon.astro` has no outbound links (Phase 3h's "no documentation link" instruction) — nothing to change there.

---

## Scope

### 1. Verify (don't assume) the build output structure

Run `npx astro build`, inspect `dist/`, and confirm whether `about.astro`/`contact.astro`/`antiphon.astro` already produce `about/index.html`/`contact/index.html`/`antiphon/index.html` (directory format, matching the route map) or something else. Report what you actually found. Only set `trailingSlash: 'always'` in `astro.config.mjs` if you find the default behavior doesn't already match the brief's route map, or if you find Astro's dev-server routing behaves inconsistently without it (e.g. warnings, mismatched `Astro.url.pathname` values) — don't add it reflexively if the default already does the job.

### 2. Rename the legal-page routes (mechanics only, no content changes)

- `src/pages/privacy-policy.astro` → `src/pages/privacy.astro`. Content and `title` prop unchanged.
- `src/pages/terms-of-service.astro` → `src/pages/terms.astro`. Content unchanged; `title` prop changes from `"Terms of Service"` to `"Terms of Use"`.

### 3. Update every internal link to the trailing-slash form

Per the Project Context list above: `Navigation.astro`, `Footer.astro` (including its two now-renamed legal-page links), and `index.astro`'s two Antiphon CTAs. Use `/antiphon/`, `/about/`, `/contact/`, `/privacy/`, `/terms/` consistently.

After this change, confirm `Navigation.astro`'s `isActive` function (`href === "/" ? currentPath === "/" : currentPath.startsWith(href)`) still correctly highlights the active nav item — build the site and check the logic against what `Astro.url.pathname` actually returns for each route with the trailing slash in place. This is a real thing to verify, not assume works.

### 4. Add `noindex` support to `BaseLayout.astro`

Add an optional `noindex?: boolean` prop (default `false`). When `true`, render `<meta name="robots" content="noindex" />` in `<head>`, in addition to the existing `<meta name="description">`. This is the only SEO-related change in this phase — canonical URLs, sitemap, `robots.txt`, and Open Graph tags are Phase 3l's job, not this one.

### 5. Create the 404 page — `src/pages/404.astro`

Content (verbatim, LOCKED):

- Heading: **404 — Page Not Found**
- Body: **The page you're looking for isn't here.**
- CTA: **Return Home**, routing to `/` — use the existing `Button` component, `variant="primary"`.

Use `BaseLayout` (full Navigation + Footer, normal design system — the brief explicitly says use the normal visual system, no special illustration or animation needed) with `title="Page Not Found"` and `noindex={true}` (Scope item 4).

Astro treats `src/pages/404.astro` as a special filename and should emit it as `dist/404.html` at the site root regardless of `build.format` — this is what GitHub Pages looks for automatically on an unmatched path, with no further hosting configuration needed. **Confirm this is actually what happened in your build output** rather than assuming Astro's documented behavior held for this version (`astro` `^7.2.0` per `package.json`).

---

## Files Allowed to Change

- `astro.config.mjs` (only if Scope item 1's verification shows it's needed)
- `src/pages/privacy-policy.astro` → renamed `src/pages/privacy.astro`
- `src/pages/terms-of-service.astro` → renamed `src/pages/terms.astro`
- `src/components/Navigation.astro`
- `src/components/Footer.astro`
- `src/pages/index.astro`
- `src/layouts/BaseLayout.astro`
- `src/pages/404.astro` (new)

If hitting any Scope item genuinely requires touching a file not listed, stop and report rather than expanding scope silently.

---

## Files and Folders Out of Scope

- Any page content beyond what Scope items 2 and 5 specify — the legal pages keep their exact current placeholder text, nothing is filled in.
- `src/pages/antiphon.astro`, `about.astro`, `contact.astro` content — untouched (Phase 3h already finalized these; this phase only touches other files' *links to* them).
- SEO metadata beyond the single `noindex` prop (Scope item 4) — no canonical tags, sitemap, `robots.txt`, or OG tags. Phase 3l.
- Font self-hosting. Phase 3k.
- Any redirect mechanism for the now-dead `/products/antiphon` or `/documentation` URLs — GitHub Pages has no server-side redirect capability without extra tooling, this site isn't live/indexed yet, and the new 404 page is the correct landing experience for both. Don't build a client-side JS redirect or a `_redirects`-style file (GitHub Pages doesn't support the latter anyway).
- Do not commit or push. The user will handle source control manually.

---

## Verification Steps

```bash
npx astro check
npx astro build
```

Both must report clean. Then:

- Inspect `dist/` directly: confirm `about/index.html`, `contact/index.html`, `antiphon/index.html`, `privacy/index.html`, `terms/index.html`, and a root-level `404.html` all exist, and that `products/` and `documentation/` do not.
- Grep `src/` for any remaining `/privacy-policy`, `/terms-of-service`, or any non-trailing-slash internal link to `/antiphon`, `/about`, or `/contact`, to confirm nothing was missed.
- Confirm the active-nav-highlight logic (Scope item 3) still works for each of the four nav items.
- If no live browser is available (the same limitation prior phases have hit), a calculated/traced confirmation is fine — state clearly which kind of evidence you're reporting.

---

## Post-Completion Review (bounded — one round)

Run **one** Codex review of the changed files if available: `codex exec --skip-git-repo-check "<files inlined verbatim>"` (the working pattern from `methodology/LESSONS_LEARNED.md`, 2026-08-09 entry).

1. Fix only findings with a concrete failure scenario. Style/preference findings are recorded, never acted on.
2. One fix pass, no re-review loop.
3. If Codex is unavailable, say so and skip this section.

---

## Documentation Updates

- [ ] None expected beyond what's already covered by this phase's own scope.

---

## Completion Report Requirements

1. Summary — what changed, by scope item.
2. What Scope item 1's build-output check actually found, and whether `trailingSlash`/`build.format` needed changing.
3. Confirmation of the `dist/` structure (Verification Steps) — list what's actually there.
4. Files changed, including the two renames.
5. The grep-for-stale-references result.
6. Review outcome (Codex findings fixed/recorded/skipped-with-reason).
7. Any decision you made where a reasonable person might have chosen differently — flag explicitly.
8. Out-of-scope items discovered — flag, do not implement.
9. Suggested follow-up tasks.

Wolfgang created this prompt. This project's own Claude Code session performs the implementation; Wolfgang does not execute implementation steps.

---

## Result File

Save a copy of your completion report to:

```
docs/Results/Phase-3i-Route-Restructure-404.md
```

---

## After This Prompt Executes

Bring the completion report back to Wolfgang. Wolfgang will update:

- `projects/clockwork-otter-website.md` — project card
- `prompts/PROMPT_LOG.md` — mark `CLOCKWORK-P3I` executed/processed
- `ecosystem/ACTIVE_PRIORITIES.md` / `NOW.md` — advance to Phase 3j
