# Prompt Log

Lifecycle record for implementation prompts on the Clockwork Otter Foundry website. One row per phase. Lifecycle: `placed → executed → processed`. **placed** = prompt written here; **executed** = a matching file exists in `docs/Results/`; **processed** = result reviewed and this log updated.

This log is local to the website repository. **As of 2026-08-07 this project is also tracked in Wolfgang's own ecosystem `PROMPT_LOG.md`/`PROJECT_REGISTRY.md`** (`../../Wolfgang/projects/clockwork-otter-website.md`), at the user's explicit request, for the same prompt-lifecycle discipline as the rest of the HowlingHound portfolio — even though Clockwork Otter Foundry remains a separate commercial venture, not part of that game/TTRPG ecosystem itself. This file stays as the local, repo-native copy of the same record.

| Phase | Prompt | Result | Status | Notes |
|-------|--------|--------|--------|-------|
| 1 — Foundation | `Phase-1-Foundation.md` | `Phase-1-Foundation.md` | **closed** 2026-08-07 | Astro 7 + Tailwind v4 (CSS-first `@theme`, no `tailwind.config.js`) scaffold: full token set in `src/styles/tokens.css`, Google Fonts CDN loading (exact approved weights only, centralized in `BaseLayout.astro`), Navigation + Footer, 7 placeholder routes (5 scoped + Privacy Policy/Terms of Service added to give the footer real links). Independently verified: `astro check` (0/0/0) and `astro build` (7 pages) both reproduced clean; logo PNG confirmed by direct inspection to be a square, vertically-stacked lockup on opaque white with no horizontal/reversed variant, matching the report. Also noticed independently (not blocking): the master PNG's wordmark is set in a serif face, not Bebas Neue as `03-logo-system.md` specifies — a brand-repo asset/spec mismatch, flagged to the user, not a website defect. **User decisions recorded 2026-08-07, now in `CLAUDE.md`'s Standing Decisions section:** (1) defer logo variants — keep the current stacked PNG as-is, do not alter/redesign it, but structure layout so a future horizontal variant is a drop-in swap; (2) keep both footer legal pages, clearly-marked placeholder content only, never generated legal text; (3) Google Fonts CDN through development, self-hosting evaluated at Phase 6, keep loading centralized (already true — confirmed only in `BaseLayout.astro`). Phase 1 fully approved. |
| 2 — Home Page | `Phase-2-Home-Page.md` | `Phase-2-Home-Page.md` | **closed** 2026-08-07 | Charcoal hero (motto + primary CTA), intro section, 3-slot product overview grid, closing secondary-CTA section, new `Button.astro` component + deferred Button tokens added to `tokens.css`. `astro check`/`astro build` both clean. Logo omitted from hero (opaque-white-bg asset would reproduce the nav's flagged contrast issue at larger scale) — **reviewed and approved 2026-08-07**, no white-card workaround wanted, deferred to a future logo variant. Not fully closed on follow-ups: no live browser check across Phases 1–2 and the still-missing Button component spec doc remain open, tracked in Wolfgang's `ecosystem/ACTIVE_PRIORITIES.md`. |
| 3 — Products | `Phase-3-Products.md` | `Phase-3-Products.md` | **processed** 2026-08-09 | New `ProductCard` component (white-surface elevation, no new tokens needed). `/products` overview page (3 placeholder products) + one static individual product page (`example-product.astro`, chosen over a dynamic `[slug].astro` to avoid inventing catalog structure) implementing the full 7-step Product Pages sequence. Home page's Phase-2 throwaway product markup replaced with `ProductCard`; all cards site-wide link to the one example page. `astro check`/`astro build` both clean (15 files / 8 pages). Bounded Codex review run (`codex exec`, no git repo so diff-based invocation wasn't available — changed files passed directly instead): no findings meeting the concrete-failure-scenario bar. |
| 3b — Logo Integration | `Phase-3b-Logo-Integration.md` | `Phase-3b-Logo-Integration.md` | **executed** 2026-08-09 | Interstitial phase: swapped the old opaque-white PNG for three new two-tone (`#000000`/`#ffffff`) production SVGs — horizontal in nav (180×45, hits the 180px min-width guidance), vertical in hero (160px mobile / 224px desktop), mark-only as a new favicon (none existed before). Old PNG deleted after confirming no remaining references. `astro check`/`astro build` both clean (15 files / 8 pages). **Live legibility check performed via pixel-accurate `sharp` composites over real Foundry Charcoal** (real headless-browser screenshot blocked by missing OS shared libs — `libnspr4`/`libnss3`/`libnssutil3`/`libasound.so.2` — with no sudo available to install them; documented precisely rather than waved off as "no GUI"). **Finding: the SVGs' black elements (gear silhouette + full wordmark) contrast at only 1.48:1 against Charcoal — below WCAG 2.2's 3:1 non-text minimum — while white elements contrast at 14.16:1.** No workaround applied per standing instruction; recorded as a new Standing Decision in `CLAUDE.md` and flagged upstream. Codex review skipped this phase — `codex exec` hung on stdin for ~19 minutes with no output and was abandoned; manual self-review substituted (see result file §10). Awaiting review/approval before Phase 4. |
